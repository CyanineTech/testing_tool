# 飞书触发的本机 AI 排障系统 - 最终实现规范与 V2.1 -> V3 迁移说明

> 说明：
> 1. 这份文档的目标不是“讲迁移思路”，而是让 AI 可以直接按文档实现最终系统。
> 2. 文档同时描述最终目标状态和从 V2.1 到该目标状态需要变更的地方。
> 3. 如果文档与现有实现不一致，以本文档为准。
> 4. 如果文档中某项标注为“迁移变更”，表示该能力在 V2.1 中存在，但需要调整才能达到最终目标。
>
> 配套执行文档：
> - 最终目标和约束：看本文档
> - 当前实现差距、实施顺序、状态更新要求：看 [v3_implementation_tracker.md](/home/robot/amr-rcs-troubleshoot/v3_implementation_tracker.md)
> - 后续 AI 在落地代码时，必须同步更新 `v3_implementation_tracker.md` 中对应条目的完成状态与验证记录

## 1. 最终目标

### 1.1 系统目标

系统是一个由飞书触发的本机排障助手，部署在 Ubuntu 20.04 (ROS 1 Noetic) 节点上，登录用户固定为 `robot`。系统最终要实现以下能力：

1. 监听飞书群里 `@机器人` 的排障请求。
2. 根据消息内容路由到对应业务线 SOP。
3. 将 SOP 和当前上下文作为推理输入，驱动模型完成排障分析。
4. 必要时让模型申请受控知识补充或受控命令执行。
5. 通过白名单沙盒安全执行只读命令。
6. 输出最终结论回传飞书。
7. 将一次排障会话沉淀为可复用的草稿 SOP。
8. 同时保留 OpenAI SDK 主路径和 Copilot CLI 兼容路径，便于对照、回归和兜底。

### 1.2 最终原则

1. 推理引擎必须可插拔。
2. 执行能力必须统一收口到沙盒。
3. 知识库必须以 SOP 文件形式沉淀。
4. 上层业务不得感知 provider 细节。
5. 任何会影响系统安全的能力，都必须先进入白名单再执行。
6. 最终目标不是“同时跑两套协议”，而是“内部统一动作语义，对外允许不同 provider 协议”。

## 2. 现有 V2.1 与最终目标的关系

### 2.1 V2.1 已有能力

V2.1 当前已经具备以下基础：

1. 飞书入口。
2. 知识库按业务线拆分。
3. PTY / CLI 驱动的推理链路。
4. `read_knowledge`、`execute`、`report` 这类动作协议。
5. 白名单执行沙盒。
6. 草稿沉淀能力。

### 2.2 最终目标新增能力

最终目标在 V2.1 基础上新增或强化以下能力：

1. 引入 OpenAI SDK 主路径。
2. 使用 `Responses API` 的标准函数调用机制作为主工具调用机制。
3. 将推理引擎抽象为 provider，保留 Copilot 作为兼容 provider。
4. 将所有动作统一成内部标准事件，避免业务层出现两套协议。
5. 明确区分“候选知识”和“已读取知识”。
6. 更严格地约束消息结构、返回结构、草稿结构、配置结构。

### 2.3 为什么要变更

从 V2.1 迁移到最终目标时，必须变更的原因如下：

1. V2.1 的 PTY 协议容易受 prompt 长度、回显噪声和交互状态影响。
2. V3 的 OpenAI SDK 能直接提供标准化工具调用闭环，便于工程化。
3. Copilot 对接需要保留，但不应继续作为业务层主依赖。
4. 如果不统一动作语义，后续会出现两套错误码、两套调试路径、两套草稿格式。
5. 如果不把 provider 抽象出来，就无法做到 OpenAI / Copilot 之间平滑切换。

### 2.4 基于公开资料的技术选型结论

对最终架构的公开资料核验结论如下：

1. OpenAI 主路径合理，但应明确优先实现为 `Responses API`，而不是继续以 Chat Completions 作为默认实现。
2. OpenAI 工具调用路径需要启用严格 schema 约束，否则函数参数会退化为 best-effort 解析，稳定性不足。
3. 对于本系统这种“一次只允许一条受控命令”的执行模型，OpenAI 主路径应关闭并行工具调用。
4. SSH 沙盒层仅靠“命令正则 + shell 执行”可信度不够，应该使用 SSH 客户端库直接执行远程命令，并明确主机密钥校验策略。
5. Copilot 兼容路径可以保留，但它应被视为兼容与 fallback 路径，而不是最终主路径，因为它缺少像 OpenAI Responses API 这样稳定、公开、结构化的工具调用契约。

## 3. 最终架构

### 3.1 分层结构

最终系统分为五层：

1. 飞书网关
2. 知识路由层
3. 推理编排层
4. 受控执行沙盒
5. 输出与复盘层

### 3.2 最终数据流

标准数据流必须是：

1. 飞书消息进入系统。
2. 路由器识别业务域并加载 SOP。
3. 编排层组装 messages。
4. Provider 开始推理。
5. Provider 需要补证时，触发标准动作。
6. 动作由统一执行层处理。
7. 结果回填给 provider。
8. Provider 输出最终结论。
9. 飞书侧收到回复。
10. Post Mortem 生成草稿。

### 3.3 Provider 模型

系统必须支持至少两种 provider：

1. `openai_sdk`
   - V3 主路径。
   - 使用 OpenAI 官方 Python SDK。
   - 默认基于 `Responses API` 实现。
   - 通过 `function_call` / `function_call_output` 完成函数调用闭环。

2. `copilot_cli`
   - V2.1 兼容路径。
   - 继续支持 PTY / CLI 交互。
   - 继续支持 `read_knowledge`、`execute`、`report` 文本动作协议。

### 3.4 内部统一动作

无论 provider 是 OpenAI 还是 Copilot，系统内部都必须统一成以下动作：

1. `read_knowledge`
2. `secure_ssh_execute`
3. `report`

说明：

1. `read_knowledge` 用于读取白名单范围内的知识文档。
2. `secure_ssh_execute` 用于执行受控只读命令。
3. `report` 用于输出最终结论结构。

### 3.5 候选知识与已读知识分离

系统内部必须明确区分两类知识证据：

1. `knowledge_candidates`
   - 路由或检索阶段给出的候选 SOP 清单。
   - 作用是缩小问题空间。

2. `knowledge_read`
   - provider 在本轮会话中实际读取过的文档。
   - 作用是向后续推理、follow-up 和草稿沉淀说明“哪些知识真的被使用过”。

原因：

1. 候选文档不等于已读文档。
2. 如果不区分这两者，最终报告中的证据链会失真。
3. 对 Copilot 兼容路径尤其重要，因为它更依赖“先候选，再按需补读”的节奏。

## 4. 项目目录与文件职责

### 4.1 最终目录结构

```text
feishu_agent/
├── main.py
├── config.yaml
├── core/
│   ├── feishu_listener.py
│   ├── router.py
│   └── post_mortem.py
├── agent/
│   └── orchestrator.py
├── providers/
│   ├── __init__.py
│   ├── openai_sdk.py
│   └── copilot_cli.py
├── protocols/
│   ├── __init__.py
│   ├── actions.py
│   └── schemas.py
├── sandbox/
│   ├── ssh_executor.py
│   └── whitelist.json
└── knowledge/
    ├── README.md
    ├── ros/
    ├── hardware_bus/
    ├── network/
    ├── backend/
    ├── frontend/
    ├── task_dispatch/
    ├── cbs/
    └── _ai_drafts/
```

### 4.2 文件职责

#### `main.py`

负责启动、读取配置、选择 provider、组装组件并启动主流程。

#### `core/feishu_listener.py`

负责飞书事件接入、消息标准化、去重、回传回复。

#### `core/router.py`

负责把输入文本路由到具体 SOP。

#### `core/post_mortem.py`

负责将一次排障会话生成草稿并写入 `_ai_drafts/`。

#### `agent/orchestrator.py`

负责统一编排 messages、provider、动作回填和最终输出。

#### `providers/openai_sdk.py`

负责 OpenAI SDK 主路径。

#### `providers/copilot_cli.py`

负责 Copilot CLI 兼容路径。

#### `protocols/actions.py`

定义标准动作类型、动作事件结构和动作返回结构。

#### `protocols/schemas.py`

定义路由结果、执行结果、报告结果、草稿结果的标准结构。

#### `sandbox/ssh_executor.py`

负责白名单校验和安全执行。

#### `sandbox/whitelist.json`

负责维护允许执行的只读命令正则。

## 5. 核心接口规范

### 5.1 `main.py`

```python
def load_config(path: str = "config.yaml") -> dict:
    ...

def build_components(config: dict):
    ...

def main() -> None:
    ...
```

要求：

1. `load_config` 只负责读取配置。
2. `build_components` 只负责组装依赖。
3. `main` 只负责启动。

### 5.2 `core/feishu_listener.py`

```python
class FeishuListener:
    def __init__(self, config: dict, router, orchestrator, post_mortem):
        ...

    def start(self) -> None:
        ...

    def handle_event(self, event: dict) -> None:
        ...

    def parse_message(self, event: dict) -> dict:
        ...

    def send_reply(self, chat_id: str, content: str) -> None:
        ...
```

要求：

1. 只负责飞书接入和回传。
2. 不直接执行命令。
3. 不直接调用 provider。
4. 必须做消息去重。

### 5.3 `core/router.py`

```python
class Router:
    def __init__(self, knowledge_root: str):
        ...

    def route(self, text: str) -> dict:
        ...

    def match_domain(self, text: str) -> str:
        ...

    def resolve_sop_path(self, domain: str) -> str:
        ...

    def load_sop(self, sop_path: str) -> str:
        ...
```

返回结构必须至少包含：

```python
{
    "domain": "hardware_bus",
    "sop_path": ".../knowledge/hardware_bus/can_bus_error.md",
    "sop_text": "..."
}
```

### 5.4 `agent/orchestrator.py`

```python
class Orchestrator:
    def __init__(self, provider, router, executor, post_mortem):
        ...

    def run(self, user_text: str, sop_text: str, context: list | None = None) -> dict:
        ...

    def format_final_answer(self, response) -> str:
        ...
```

要求：

1. `Orchestrator` 不直接关心 provider 是 OpenAI 还是 Copilot。
2. `run()` 必须完成一轮及以上动作闭环。
3. `run()` 必须返回最终答案、messages、tool trace、sop_path、domain。
4. `run()` 必须同时追踪 `knowledge_candidates` 和 `knowledge_read`。
5. `run()` 必须为每个动作生成唯一 trace 记录，至少包含时间、provider、动作类型、是否成功。

### 5.5 `providers/openai_sdk.py`

```python
class OpenAIProvider:
    def __init__(self, base_url: str, api_key: str, executor):
        ...

    def run(self, messages: list, tools: list) -> dict:
        ...
```

要求：

1. 使用官方 `openai` Python SDK。
2. 主实现必须使用 `client.responses.create(...)`，不要把 Chat Completions 作为默认主实现。
3. 支持自定义 `base_url` 和 `api_key`。
4. 必须解析 `function_call` 与 `function_call_output`。
5. 工具 schema 必须启用严格模式。
6. 工具 schema 中每个 object 参数都必须显式声明 `required` 和 `additionalProperties: false`。
7. 必须显式设置 `parallel_tool_calls: false`，因为本系统一次只允许一条受控命令。
8. 默认应使用 `store: false`，除非部署方明确需要状态持久化。
9. 多轮对话必须明确选择上下文策略：要么使用 `previous_response_id`，要么手动回传已处理过的 input/output items，不能混用得不透明。
10. 最终结构化报告优先使用 Responses API 的结构化文本输出能力，而不是依赖自然语言后解析。
11. provider 可以接收上层传入的通用 `messages`，但内部必须明确转换为 Responses API 使用的 `instructions` 与 `input` items。
12. 如果最终报告使用 JSON schema，必须在 `text.format` 中显式声明 `strict: true`。

推荐实现语义：

```python
response = client.responses.create(
    model=model,
    instructions=system_instructions,
    input=input_items,
    tools=tools,
    parallel_tool_calls=False,
    store=False,
)
```

### 5.6 `providers/copilot_cli.py`

```python
class CopilotProvider:
    def __init__(self, config: dict, executor):
        ...

    def run(self, messages: list, tools: list | None = None) -> dict:
        ...
```

要求：

1. 保留 PTY / CLI 交互。
2. 保留 `read_knowledge`、`execute`、`report` 文本协议。
3. 只作为兼容和 fallback 路径。
4. 不允许绕过沙盒直接执行命令。
5. prompt 必须使用摘要版 SOP，不要直接灌入完整长文档。
6. provider 输出必须被适配为与 OpenAI 主路径一致的内部动作事件。
7. 不要求 Copilot 路径与 OpenAI 路径完全等价，但要求输出结构等价。

### 5.7 `sandbox/ssh_executor.py`

```python
class SSHExecutor:
    def __init__(self, config: dict, whitelist_path: str):
        ...

    def load_whitelist(self) -> dict:
        ...

    def validate_command(self, command: str) -> tuple[bool, str]:
        ...

    def execute(self, command: str) -> dict:
        ...
```

要求：

1. 默认用户必须是 `robot`。
2. 命令必须先校验，再执行。
3. 执行超时固定 10 秒。
4. 返回结果必须结构化。
5. 不允许危险命令。
6. 不允许通过 shell 拼接 `ssh ...` 命令再执行。
7. 必须使用 SSH 客户端库直接建立连接并执行命令，例如 Paramiko 或同类库。
8. 必须加载系统或本地已知主机密钥，并拒绝未知主机密钥，不能自动信任新主机。
9. 每次执行后必须显式关闭 SSH 连接，不能依赖垃圾回收。

### 5.8 `core/post_mortem.py`

```python
class PostMortem:
    def __init__(self, drafts_dir: str):
        ...

    def generate_draft(self, conversation: list, answer: str, sop_path: str) -> str:
        ...

    def build_draft_text(self, conversation: list, answer: str, sop_path: str) -> str:
        ...

    def save_draft(self, text: str, filename: str | None = None) -> str:
        ...
```

要求：

1. 草稿只写入 `_ai_drafts/`。
2. 草稿必须保留 SOP 路径、关键命令和最终结论。
3. 草稿不得覆盖正式 SOP。

## 6. 标准数据结构

### 6.1 路由结果

```python
{
    "domain": "hardware_bus",
    "sop_path": "...",
    "sop_text": "...",
    "matched_keywords": ["..."],
    "route_reason": "..."
}
```

### 6.2 动作事件

```python
{
    "type": "read_knowledge" | "secure_ssh_execute" | "report",
    "action_id": "...",
    "payload": {...},
    "source": "openai_sdk" | "copilot_cli",
    "created_at": "..."
}
```

### 6.3 执行结果

```python
{
    "ok": True,
    "type": "secure_ssh_execute",
    "command": "ip -details link show can0",
    "stdout": "...",
    "stderr": "...",
    "return_code": 0,
    "duration_ms": 123,
    "provider": "openai_sdk",
    "matched_rule": "^ip -details link show [a-zA-Z0-9_-]+$",
    "host": "robot@target-host",
    "rejection_reason": null
}
```

### 6.4 最终报告

```python
{
    "summary": "...",
    "root_cause": "...",
    "severity": "info" | "warning" | "critical",
    "next_steps": ["..."],
    "evidence": ["..."],
    "confidence": 0.82,
    "human_intervention_required": true,
    "knowledge_used": ["knowledge/hardware_bus/can_bus_error.md"],
    "commands_used": ["ip -details link show can0"]
}
```

### 6.5 草稿结果

```python
{
    "draft_path": ".../_ai_drafts/xxx.md",
    "title": "...",
    "source_sop": "...",
    "created_at": "..."
}
```

## 7. 知识库规范

### 7.1 SOP 目录职责

知识库必须按业务线垂直拆分。每条 SOP 需要明确：

1. 适用场景。
2. 典型现象。
3. 核心排查命令。
4. 根因判别矩阵。
5. 飞书回复建议。
6. 禁止操作。

### 7.2 SOP 固定模板

```markdown
# <故障名称> 排查 SOP

## 1. 适用场景
一句话描述触发场景。

## 2. 典型现象
列出用户能看到的症状或日志特征。

## 3. 核心排查命令
1. 第一条命令
2. 第二条命令
3. 第三条命令

## 4. 结果判别
| 现象/日志特征 | 根因判定 | 飞书回复方案 |
| :--- | :--- | :--- |
| ... | ... | ... |

## 5. 飞书回复
给出明确、可执行、面向现场的建议。

## 6. 禁止操作
列出不应由 AI 主动建议执行的高风险操作。
```

### 7.3 SOP 编写要求

1. 命令必须可被白名单覆盖。
2. 命令必须可执行。
3. 命令必须尽量使用标准固定形态。
4. 不得写会导致写盘、删文件、重启的命令。
5. 如果 SOP 中使用管道或过滤器，白名单必须同步允许该完整命令形态。
6. 每条命令都应写明“看什么输出”与“该输出意味着什么”。
7. 对 OpenAI 主路径，SOP 应优先帮助模型决定是否调用工具，而不是在提示词中塞过多长日志样例。

## 8. 白名单与安全边界

### 8.1 白名单原则

`sandbox/whitelist.json` 是系统安全边界，命令必须先通过它，再执行。

除正则白名单外，执行层还必须做第二层约束：

1. 对命令做字符串归一化。
2. 拒绝危险元字符。
3. 明确禁止 `shell=True` 风格执行。
4. 保证“一次动作只对应一条命令”。

### 8.2 允许模式示例

```json
{
  "ros": [
    "^rostopic hz.*$",
    "^rosnode info /[a-zA-Z0-9_/-]+$",
    "^rosnode ping /[a-zA-Z0-9_/-]+$"
  ],
  "hardware_bus": [
    "^ip -details link show [a-zA-Z0-9_-]+$",
    "^dmesg.*$",
    "^lsusb.*$",
    "^candump.*$"
  ],
  "network": [
    "^systemctl status [a-zA-Z0-9_.@-]+$",
    "^ping -c [1-5] .*$",
    "^ip a.*$"
  ],
  "backend": [
    "^journalctl.*$",
    "^mount.*$",
    "^ps.*$",
    "^top.*$"
  ],
  "frontend": [
    "^journalctl.*$",
    "^ps.*$"
  ],
  "task_dispatch": [
    "^journalctl.*$",
    "^ps.*$"
  ],
  "cbs": [
    "^journalctl.*$",
    "^ntpq.*$",
    "^mosquitto_sub.*$"
  ]
}
```

### 8.3 明确拒绝的输入

任何包含以下内容的命令都必须直接拒绝：

1. `>`
2. `<`
3. `| rm`
4. `usermod`
5. `chmod`
6. `reboot`
7. `shutdown`

### 8.4 超时要求

1. 执行超时固定 10 秒。
2. 如需修改，只能通过 `config.yaml` 显式配置。
3. 不得在代码中硬编码不同的临时值。

## 9. Provider 兼容策略

### 9.1 为什么要保留 Copilot

保留 Copilot 不是因为它是主路径，而是因为它有三个价值：

1. 兼容现有 V2.1 行为。
2. 在 OpenAI 不可用时作为 fallback。
3. 作为对照，帮助定位新旧实现差异。

### 9.2 兼容边界

Copilot provider 只能负责“推理”，不能绕过以下边界：

1. 不能绕过 `sandbox/ssh_executor.py`。
2. 不能绕过路由器直接看全库。
3. 不能绕过统一 report 结构。
4. 不能直接访问本地文件系统。

### 9.3 统一原因

必须统一动作语义的原因是：

1. 避免双协议并存。
2. 避免两套调试口径。
3. 避免白名单与 SOP 分叉。
4. 避免草稿格式不统一。

## 10. `config.yaml` 约束

### 10.1 必备配置

```yaml
provider:
  mode: openai_sdk
  fallback_mode: copilot_cli

llm:
  base_url: "..."
  api_key: "..."
  model: "..."

copilot:
  enabled: true
  pty_protocol_enabled: true

ssh:
  host: "..."
  user: "robot"
  timeout_seconds: 10
  default_interface: "enp88s0"

system:
  default_user: "robot"
  knowledge_root: "knowledge"
  drafts_root: "knowledge/_ai_drafts"
  log_root: "logs"
```

### 10.2 配置规则

1. `system.default_user` 必须为 `robot`。
2. `ssh.timeout_seconds` 默认 10。
3. provider mode 允许在部署时切换。
4. 新增配置项必须先写进文档，再写进代码。
5. 建议显式增加 `llm.store_responses: false`、`llm.parallel_tool_calls: false`、`ssh.reject_unknown_host_keys: true`。

## 11. 从 V2.1 迁移到最终目标时必须变更的点

### 11.1 变更清单

#### 变更 1：引入 provider 抽象

**现状：** V2.1 里推理逻辑主要围绕本地 CLI / PTY 交互。  
**最终目标：** 推理引擎必须抽象成 `provider`。  
**为什么要变：** 为了支持 OpenAI 主路径，同时保留 Copilot fallback，避免业务层耦合具体 provider。

#### 变更 2：将 Copilot 变成兼容 provider

**现状：** Copilot 在 V2.1 中更像主引擎。  
**最终目标：** Copilot 只作为兼容 provider。  
**为什么要变：** V3 的主路径需要标准工具调用，Copilot 只保留用于对照、兜底和迁移缓冲。

#### 变更 3：统一动作协议

**现状：** V2.1 使用文本代码块协议。  
**最终目标：** 内部统一成 `read_knowledge`、`secure_ssh_execute`、`report`。  
**为什么要变：** 统一语义，减少 provider 差异导致的上层分叉。

#### 变更 4：改造推理入口

**现状：** V2.1 偏向 PTY 对话驱动。  
**最终目标：** OpenAI 使用 `Responses API` 的标准工具调用闭环。  
**为什么要变：** 官方文档已将 Responses API 作为新项目推荐路径，它对工具调用、多轮状态和结构化输出更直接、更稳定。

#### 变更 5：收紧知识库与白名单对齐

**现状：** SOP 与白名单可能只做了粗粒度对齐。  
**最终目标：** SOP 中的命令必须完全可被白名单放行。  
**为什么要变：** 避免“知识库要求执行，但沙盒拒绝执行”的死锁。

#### 变更 6：统一输出结构

**现状：** 不同 provider 的返回格式可能不同。  
**最终目标：** 路由结果、执行结果、报告结果、草稿结果都必须有统一结构。  
**为什么要变：** 为了让上层业务只处理统一对象，不处理 provider 差异。

### 11.2 不需要变更的点

以下模块应尽量保持单一实现，不要因为迁移而拆出两套版本：

1. `core/router.py`
2. `sandbox/ssh_executor.py`
3. `core/post_mortem.py`
4. `knowledge/` 目录结构

这些模块本质上是业务能力，不是 provider 能力。

## 12. 实施顺序

### 12.1 第一阶段：抽象接口

目标：先把 V2.1 的推理入口抽象成统一 provider 接口。

步骤：

1. 提取 `LLMProvider` 接口。
2. 把现有 Copilot 逻辑迁入 `copilot_cli.py`。
3. 保持原有行为不变。

验收：

1. 旧场景可继续工作。
2. 不影响飞书入口。
3. 不影响沙盒和草稿。

### 12.2 第二阶段：引入 OpenAI 主路径

目标：让 OpenAI SDK 成为主路径。

步骤：

1. 实现 `openai_sdk.py`。
2. 把 `Responses API` 的函数调用事件映射到内部标准动作。
3. 保持沙盒与草稿结构不变。

验收：

1. OpenAI 路径可完整闭环。
2. 执行结果结构稳定。
3. 最终报告结构稳定。

### 12.3 第三阶段：统一结果与草稿格式

目标：让 provider 切换不影响上层。

步骤：

1. 统一 action schema。
2. 统一 execution result。
3. 统一 report result。
4. 统一 post mortem 模板。

验收：

1. provider 切换不改业务层。
2. 草稿格式稳定。
3. 飞书回复格式稳定。

## 13. 验收标准

最终实现必须满足以下检查点：

1. `FeishuListener` 能处理飞书消息并去重。
2. `Router` 能返回 SOP 路径与内容。
3. `Orchestrator` 能驱动至少一轮动作闭环。
4. `OpenAIProvider` 能通过 `Responses API` 的函数调用机制触发沙盒执行。
5. `CopilotProvider` 能保留兼容路径。
6. `SSHExecutor` 能拒绝危险命令并返回明确原因。
7. `PostMortem` 能生成草稿并写入 `_ai_drafts/`。
8. 输出给飞书的最终回复必须包含结论、建议动作和是否需要人工介入。
9. 工具调用 trace 中必须能区分 `knowledge_candidates`、`knowledge_read`、`secure_ssh_execute` 三类证据。

## 14. 风险说明

### 14.1 双协议并存风险

如果把 Copilot 的文本协议和 OpenAI 的 tool calling 同时暴露在业务层，会出现两套逻辑。必须避免。

### 14.2 白名单与 SOP 不对齐风险

如果 SOP 命令没有被白名单允许，系统会出现“知道怎么做却不能做”的阻塞。必须在实现前对齐。

### 14.3 prompt 膨胀风险

Copilot 路径对 prompt 长度更敏感，因此 Copilot provider 应使用摘要版 prompt，OpenAI provider 使用完整 SOP + tool schema。

### 14.4 输出结构漂移风险

不同 provider 的输出如果不能统一，后面就会出现调试困难和草稿格式不一致。必须统一 schema。

## 15. 结论

这份文档定义的是最终可落地实现，不只是迁移说明。  
对于实现者来说，正确的理解方式是：

1. 先按本文档实现最终系统结构。
2. 再用“变更清单”标记哪些地方是从 V2.1 迁移来的。
3. 迁移的目的不是改名，而是把原来依赖 PTY 的主路径升级为可插拔 provider 架构，同时保留 Copilot 的兼容能力。

如果只看这一份文档，也应该能实现出最终系统。
